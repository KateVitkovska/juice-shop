import { Then } from '@wdio/cucumber-framework';

Then(/^I submit '(.*?)' in search bar$/, async function (item) {
    
})

Then(/^I add items to basket$/, async function (items) {
    
})

Then(/^I see items in basket$/, async function (items) {
    
})

Then("I add {string} to basket", async function (item) {
    
})

Then("I see total price is {string}", async function (price) {
    
})
